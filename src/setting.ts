export interface Settings {
    title: string,
    showSettings: boolean,
    tagsView: boolean
}

const settings: Settings = {
    title: "",
    showSettings: false,
    tagsView: false
}

export default settings;