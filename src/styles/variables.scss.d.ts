/**
 * ts不能识别scss导出，需要导出scss声明文件
 */
export interface ScssVariables {
    menuText: string;
    menuActiveText: string;
    subMenuActiveText: string;
    menuBg: string;
    menuHover: string;
    subMenuBg: string;
    subMenuHover: string;
    sideBarWidth: string;
}

const variables: ScssVariables
export default variables